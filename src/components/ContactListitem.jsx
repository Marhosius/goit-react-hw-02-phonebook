import { Component } from "react";
import PropTypes from "prop-types";

export class ContactListitem extends Component {
    render() {
        return (
            <button className="">ContactListitem</button>
        )
    }
}

ContactListitem.propTypes = {
}

export default ContactListitem