import { Component } from "react";
import PropTypes from "prop-types";

export class ContactList extends Component {
    render() {
        return (
            <button className="">ContactList</button>
        )
    }
}

ContactList.propTypes = {
}

export default ContactList